<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rico - <%= artigo.titulo %></title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    
    <link rel="stylesheet" href="/assets/css/style.css">

    <style>
        /* Adicionado para remover a margem padrão do navegador */
        body {
            margin: 0;
        }

        body.dashboard-page {
            background: #1E1B2E !important;
        }
        
        nav.dashboard-nav {
            padding-top: 1.5rem;
            padding-bottom: 1.5rem;
            background-color: #2D234D;
            border-bottom: 2px solid #FFC107;
            width: 100%;
        }
    </style>
</head>
<body class="dashboard-page">

    <nav class="dashboard-nav">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="/dashboard" class="text-white text-decoration-none fs-5 fw-bold">Rico</a>
            <a href="/logout" class="btn btn-outline-light">Logout</a>
        </div>
    </nav>
    
    <a href="/artigos" class="btn btn-outline-light mt-5 ms-5">&larr; Voltar aos artigos</a>

    <div class="container py-5 article-container-wrapper">
        <div class="row justify-content-center">
            <div class="col-12 col-lg-9">
                <article class="text-white">
                    <header class="mb-4 text-center">
                        <h1 class="fw-bold text-warning mb-3" style="font-size: 2.8rem; line-height: 1.2;">
                            <%= artigo.titulo %>
                        </h1>
                        <p class="lead text-white"><%= artigo.resumo %></p>
                        
                        <div class="d-flex flex-wrap align-items-center justify-content-center gap-4 mt-4 pb-3 border-bottom border-warning">
                            <div class="d-flex align-items-center">
                                <i class="bi bi-person-circle fs-4 me-2 text-warning"></i>
                                <div>
                                    <small class="text-white-50 d-block">Autor</small>
                                    <span class="fw-bold"><%= artigo.autor %></span>
                                </div>
                            </div>
                            <div class="d-flex align-items-center">
                                <i class="bi bi-calendar-event fs-4 me-2 text-warning"></i>
                                <div>
                                    <small class="text-white-50 d-block">Publicado em</small>
                                    <span class="fw-bold"><%= artigo.data %></span>
                                </div>
                            </div>
                        </div>
                    </header>
                    <div class="article-content" style="font-size: 1.1rem; line-height: 1.8;">
                        <%- artigo.conteudo %>
                    </div>
                </article>
            </div>
        </div>
    </div>
</body>
</html>